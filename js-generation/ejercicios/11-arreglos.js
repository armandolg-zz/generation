const arreglo = [];
const arreglo2 = new Array();

let lenguajes = ["Javascript", "Java", "Java", "PHP", "Python"];

lenguajes.forEach(function (lnguaje) {
  document.write(`<p>${index} - ${lenguaje}</p>`);
});
document.write(`</ul>`);

let numeros = Array(2, 3, 4, 27, 19, 12);
